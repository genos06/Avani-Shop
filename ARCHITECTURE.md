# Deployment Architecture

## How Everything Connects

```
┌─────────────────────────────────────────────────────────────────┐
│                         YOUR USERS                               │
│                                                                  │
│  Browser visits: https://yourdomain.com                         │
└────────────────────────┬─────────────────────────────────────────┘
                        │
                        │ (HTTPS Request)
                        │
┌───────────────────────▼──────────────────────────────────────────┐
│                    GODADDY DNS                                    │
│                                                                   │
│  DNS Records:                                                     │
│  • yourdomain.com      → avanii-shop.onrender.com                │
│  • www.yourdomain.com  → avanii-shop.onrender.com                │
└────────────────────────┬──────────────────────────────────────────┘
                        │
                        │ (Routes traffic)
                        │
┌───────────────────────▼──────────────────────────────────────────┐
│                   RENDER PLATFORM                                 │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │              WEB SERVICE (Flask App)                         ││
│  │                                                              ││
│  │  • Python 3.11                                               ││
│  │  • Gunicorn Server                                           ││
│  │  • Your Flask Application (main.py)                          ││
│  │  • Auto-SSL Certificate (HTTPS)                              ││
│  │                                                              ││
│  │  URL: https://avanii-shop.onrender.com                       ││
│  └────────────────────┬─────────────────────────────────────────┘│
│                       │                                          │
│                       │ (Database Queries)                       │
│                       │                                          │
│  ┌────────────────────▼─────────────────────────────────────────┐│
│  │          POSTGRESQL DATABASE                                 ││
│  │                                                              ││
│  │  • Users                                                     ││
│  │  • Products                                                  ││
│  │  • Categories                                                ││
│  │  • Orders                                                    ││
│  │  • Cart Items                                                ││
│  │                                                              ││
│  │  Database: avanii_production                                 ││
│  └──────────────────────────────────────────────────────────────┘│
└───────────────────────────────────────────────────────────────────┘
```

## Data Flow

### 1. User Visits Website
```
User → GoDaddy DNS → Render → Flask App → Returns HTML
```

### 2. User Adds Product to Cart
```
User clicks "Add to Cart" 
→ Flask processes request 
→ Saves to PostgreSQL 
→ Returns updated cart
```

### 3. User Places Order
```
User submits order form 
→ Flask validates data 
→ Creates order in PostgreSQL 
→ Clears cart 
→ Shows confirmation
```

## GitHub → Render Workflow

```
┌─────────────────────────────────────────────────────────┐
│  YOUR COMPUTER                                           │
│                                                          │
│  1. You write code                                       │
│  2. git add .                                            │
│  3. git commit -m "message"                              │
│  4. git push origin main                                 │
└────────────────────┬─────────────────────────────────────┘
                    │
                    │ (Pushes code)
                    │
┌───────────────────▼──────────────────────────────────────┐
│  GITHUB                                                   │
│                                                           │
│  Repository: yourusername/avanii-shop                     │
│  • Stores your code                                       │
│  • Version control                                        │
│  • Connected to Render                                    │
└────────────────────┬──────────────────────────────────────┘
                    │
                    │ (Auto-deploys on new commits)
                    │
┌───────────────────▼──────────────────────────────────────┐
│  RENDER                                                   │
│                                                           │
│  1. Detects new commit                                    │
│  2. Pulls latest code                                     │
│  3. Runs: pip install -r requirements.txt                 │
│  4. Starts: gunicorn main:app                             │
│  5. Your site is live! 🎉                                 │
└───────────────────────────────────────────────────────────┘
```

## Environment Variables Flow

```
┌─────────────────────────────────────────────────────────┐
│  RENDER DASHBOARD                                        │
│                                                          │
│  Environment Variables:                                  │
│  ┌────────────────────────────────────────────────────┐ │
│  │ SECRET_KEY = 830e5b889...                          │ │
│  │ DATABASE_URL = postgresql://...                    │ │
│  │ FLASK_ENV = production                             │ │
│  └────────────────────────────────────────────────────┘ │
└────────────────────┬─────────────────────────────────────┘
                    │
                    │ (Injected at runtime)
                    │
┌───────────────────▼──────────────────────────────────────┐
│  YOUR FLASK APP (main.py)                                │
│                                                           │
│  if os.environ.get('DATABASE_URL'):                      │
│      # Use PostgreSQL (Production)                       │
│  else:                                                    │
│      # Use SQLite (Local Development)                    │
└───────────────────────────────────────────────────────────┘
```

## Development vs Production

```
┌─────────────────────────────────────────────────────────────┐
│                    LOCAL DEVELOPMENT                         │
│                                                              │
│  • Database: SQLite (site.db file)                          │
│  • Server: Flask development server                         │
│  • Port: 5001                                                │
│  • Debug: True                                               │
│  • URL: http://localhost:5001                               │
└──────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    PRODUCTION (RENDER)                       │
│                                                              │
│  • Database: PostgreSQL (managed by Render)                 │
│  • Server: Gunicorn (production WSGI server)                │
│  • Port: Managed by Render                                   │
│  • Debug: False                                              │
│  • URL: https://yourdomain.com                              │
│  • SSL: Automatic (free Let's Encrypt certificate)          │
└──────────────────────────────────────────────────────────────┘
```

## Request Processing

```
User Request → Nginx (Render's Load Balancer)
              ↓
          Gunicorn (WSGI Server)
              ↓
          Flask Application
              ↓
      ┌───────┴────────┐
      ↓                ↓
Static Files      Database Queries
(CSS, JS, Images)  (PostgreSQL)
      ↓                ↓
      └───────┬────────┘
              ↓
         HTML Response
              ↓
          User's Browser
```

## Scaling Potential

```
FREE TIER:
┌────────────────────────┐
│  1 Web Service         │
│  1 PostgreSQL Database │
│  Limited CPU/RAM       │
│  Spins down after 15m  │
└────────────────────────┘

PAID TIER (if you upgrade):
┌────────────────────────┐
│  Multiple Web Services │
│  Always-on service     │
│  More CPU/RAM          │
│  Auto-scaling          │
│  Background workers    │
│  Cron jobs             │
└────────────────────────┘
```

## Security Layers

```
User Request
    ↓
[1] HTTPS/SSL (Render provides free certificate)
    ↓
[2] Flask Security (CSRF protection, session management)
    ↓
[3] Login System (Flask-Login, password hashing)
    ↓
[4] Database (Parameterized queries, SQLAlchemy ORM)
    ↓
[5] Environment Variables (Secrets not in code)
```

## Backup Strategy (Recommended)

```
┌─────────────────────────────────────────────────────────┐
│  REGULAR BACKUPS (Your responsibility)                   │
│                                                          │
│  Weekly:                                                 │
│  • Download database backup from Render                  │
│  • Store in secure location (Google Drive, Dropbox)      │
│                                                          │
│  Monthly:                                                │
│  • Full code backup (already in GitHub)                  │
│  • Uploaded images backup (if using file uploads)        │
│                                                          │
│  Before major changes:                                   │
│  • Create database backup                                │
│  • Create git branch for new features                    │
└──────────────────────────────────────────────────────────┘
```

## Monitoring

```
┌─────────────────────────────────────────────────────────┐
│  RENDER DASHBOARD                                        │
│                                                          │
│  • Real-time logs                                        │
│  • Deploy history                                        │
│  • Resource usage (CPU, RAM, bandwidth)                  │
│  • Uptime monitoring                                     │
│  • Error tracking                                        │
└──────────────────────────────────────────────────────────┘
```

This architecture provides:
✅ Scalability - Can handle growing traffic
✅ Security - Multiple layers of protection
✅ Reliability - Render's infrastructure is highly available
✅ Maintainability - Easy to update and deploy changes
✅ Cost-effective - Free for 90 days, then affordable pricing
